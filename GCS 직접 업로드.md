# GCS 직접 업로드

브라우저가 **오브젝트 스토리지(GCS)에 파일을 직접 올리고**, 서버는 그 앞뒤만 맡는 방식. 서버가 파일 바이트를 아예 안 만진다.

왜 이렇게까지 하냐면 — 파일이 서버를 거치면 그 경로에 있는 **모든 중간 장치의 제약**을 다 받는다. 리버스 프록시의 요청 본문 크기 제한에 걸려 큰 멀티파트 바디가 중간에서 잘리거나, 서버가 전체를 메모리에 올리다 죽는다. 바이트가 지나가지 않으면 이 제약들이 통째로 사라진다.

핵심 한 줄: **통제는 서버가, 운반은 브라우저가.**

```
브라우저 ──① 허가 요청──▶ 서버        (인증·인가·경로 결정 → 서명 URL 발급)
브라우저 ──② PUT────────▶ 스토리지    (파일 바이트. 서버를 안 지남)
브라우저 ──③ 완료 통보──▶ 서버        (검증 후 DB 기록)
```

여기서 배운 함정들을 모아둔다. 개념 자체보다 **밟기 쉬운 지뢰**가 많은 주제였다.

## 서명 URL = 자격증명이 아니라 인가를 넘기는 것

브라우저에 스토리지 자격증명을 내려보내면 버킷 전체가 열린다. 그래서 서버가 **조건이 박힌 일회용 URL**을 만들어 준다. 호텔 마스터키 대신 "307호만, 15분만" 열리는 카드키.

서명에 포함된 것이 곧 제약이다 — **경로**(이 파일 하나만) · **메서드**(쓰기만) · **content-type**(다른 헤더면 서명 불일치로 거부) · **만료**(유출돼도 수명이 짧다).

[[SpringSecurity]]에서 정리한 인증(누구인가)과 인가(무엇을 할 수 있나)의 구분이 여기서 아주 선명하게 나타난다. 서명 URL은 **인가만 떼어내 URL에 박아 넘기는 것**이다. 받는 쪽은 자기가 누구인지 증명할 필요가 없다 — URL 자체가 권한 증서다. [[REST API]]가 "주소로 의도를 표현"한다면, 서명 URL은 한 발 더 나가 **주소로 권한까지 표현**한다.

**저장 경로는 반드시 서버가 정한다.** 클라이언트가 고르게 두면 남의 폴더에 쓰거나 기존 파일을 덮어쓸 수 있다. 서명은 "이 경로에 쓸 권한"이라, 경로를 클라이언트가 정하면 제약이 무의미해진다.

## CORS는 인증이 아니다 — 제일 많이 헤맨 지점

서명 URL이 완벽해도 브라우저에서만 실패할 수 있다. `curl`로는 200이 나오는데 브라우저 콘솔엔 CORS 에러가 뜬다.

이유: **CORS는 브라우저가 응답 헤더를 보고 내리는 판정**이지, 서버 권한 문제가 아니다. 브라우저는 진짜 요청 전에 preflight(`OPTIONS`)를 먼저 보내는데, **여기엔 인증 정보가 실리지 않는다.** 서버도 권한을 확인하지 않고 CORS 설정만 보고 답한다. 허용 헤더가 안 붙으면 브라우저가 실제 요청을 **발사조차 하지 않는다**.

그래서 권한이 아무리 좋아도 소용이 없다. 별도 개념으로 정리할 만해서 [[CORS]]로 빼두려 한다.

당장 밟은 지뢰 둘:

- **오리진은 문자열 비교다.** 스토리지가 내 로컬로 접속하는 게 아니라 요청 헤더의 `Origin` 문자열을 목록과 대조할 뿐이다. 그래서 `localhost`도 목록에 넣으면 로컬 개발에서 그대로 통과한다. 반대로 `localhost:3000`과 `127.0.0.1:3000`은 **다른 오리진**이라 한쪽만 등록하면 다른 쪽은 막힌다.
- **GCS 버킷 CORS는 웹 콘솔에서 못 바꾼다.** CLI나 API로만 설정되고, 게다가 설정 파일을 주면 **추가가 아니라 통째로 덮어쓴다.** 기존 값을 먼저 백업하지 않으면 남의 오리진을 날린다.

## 버킷에 CORS 넣는 법 (Cloud Shell)

**프론트에서 하는 설정이 아니다.** 버킷 소유 쪽에서 넣어야 하고, 프론트 코드로는 못 푼다.

두 가지가 앞을 막는다 — 웹 콘솔에 CORS 편집 화면이 아예 없고, 앱이 쓰는 런타임 서비스 계정에는 보통 **오브젝트 권한만** 있지 버킷 설정 권한(`storage.buckets.update`)이 없다. 그래서 **관리자 계정으로 Cloud Shell**에서 하는 게 제일 빠르다. 로컬에 CLI를 깔고 로그인하는 것보다 낫다 — Cloud Shell은 CLI가 이미 있고 내 계정으로 인증된 채 열린다.

```bash
# 0) 프로젝트 확인 (버킷 이름은 전역 고유라 다른 프로젝트가 설정돼 있어도 도달은 하지만, 맞춰두는 게 덜 헷갈린다)
gcloud config get-value project
gcloud config set project <PROJECT_ID>

# 1) 현재값 백업 — --cors-file 은 덮어쓰기다. 이걸 건너뛰면 남의 오리진을 날린다
gcloud storage buckets describe gs://<BUCKET> --format='value(cors_config)' > cors.backup.txt
cat cors.backup.txt

# 2) 설정 작성. 배열이고, 각 원소가 아래 네 필드를 갖는다
cat > cors.json <<'EOF'
[{"origin":["https://app.example.com","http://localhost:3000"],"method":["PUT","GET","OPTIONS"],"responseHeader":["Content-Type"],"maxAgeSeconds":3600}]
EOF
python3 -m json.tool cors.json     # 파싱되면 문법 OK

# 3) 적용 + 반영 확인
gcloud storage buckets update gs://<BUCKET> --cors-file=cors.json
gcloud storage buckets describe gs://<BUCKET> --format='value(cors_config)'
```

필드 의미: `origin` 허용할 출처(문자열 정확히) · `method` 허용 메서드 · `responseHeader` **요청에 붙일 헤더 이름**(서명 URL 업로드는 `Content-Type`이 필수) · `maxAgeSeconds` preflight 캐시 수명.

**JSON 이 두 번 깨졌다.** 다른 곳에서 복사해 붙이면 `"` 가 곡선 따옴표 `”` 로 바뀌거나 URL 중간에 줄바꿈이 들어간다. 그래서 위처럼 **한 줄로** 쓰고 `json.tool` 로 먼저 검증한다. 이미 깨졌으면 `sed -i 's/[“”]/"/g' cors.json`.

**반영 확인은 밖에서 프리플라이트로.** 버킷 조회 권한이 없어도 되고, 실제 브라우저가 보는 것과 같은 판정을 본다.

```bash
curl -s -o /dev/null -D - -X OPTIONS "https://storage.googleapis.com/<BUCKET>/<any-path>" \
  -H "Origin: https://app.example.com" \
  -H "Access-Control-Request-Method: PUT" \
  -H "Access-Control-Request-Headers: content-type"
```

응답에 `access-control-allow-origin` 이 있으면 통과. 등록 안 된 출처로 같은 요청을 한 번 더 쏴서 **헤더가 안 붙는 것까지** 확인하면 "아무 데나 열린 것"이 아님을 검증할 수 있다. (설정이 없어도 `OPTIONS` 자체는 200 이 오므로 상태 코드만 보면 속는다.)

로컬 테스트용으로 넣은 `localhost` 는 끝나면 목록에서 빼는 게 맞다. 운영 버킷에 남겨둘 이유가 없다.

## 클라이언트가 신고한 값을 믿지 않는다

완료 통보에 브라우저가 "10MB짜리예요"라고 적어 보내도 믿을 수 없다. 조작 가능한 환경이다. 서버는 **스토리지에 직접 물어본** 실제 크기·타입을 진실의 출처로 삼는다.

여기서 얻은 설계 감각 하나 — **어차피 무시할 값은 아예 인자로 받지 않는 게 낫다.** 받아놓고 무시하면 "보냈는데 왜 안 쓰이지"라는 혼동만 남고, 언젠가 누가 편의상 그 값을 쓰게 된다. 받지 않으면 **실수로 쓸 수 있는 경로 자체가 사라진다.** 신뢰 경계를 주석이 아니라 **함수 시그니처로 강제**하는 것.

## 원자성을 잃으면 고아가 생긴다

기존에는 `업로드 + DB 기록`이 한 요청이라 둘 중 하나만 성공할 수 없었다. 쪼개는 순간 **중간 상태가 새로운 실패 모드**가 된다 — PUT은 성공했는데 완료 통보 전에 탭이 닫히면, DB에 행이 없는 파일이 스토리지에 남는다.

고약한 건 이 고아를 **제품 안에서 손댈 수 없다**는 것이다:

- 목록 조회가 DB만 보므로 화면에 **안 뜬다**
- 삭제는 DB 행에서 출발해 파일 경로를 찾아가는 구조라, 행이 없으면 **시작조차 못 한다**

기능 오류도 없고 아무도 참조하지 않는다. 증상 없이 용량만 잠식하다 청구서로 드러난다. 회수하려면 스토리지 목록과 DB를 대조하는 별도 작업이 필요하다.

> 쪼갠 연산의 중간 상태를 사후 대조 말고 **구조적으로** 막을 수 있나? 서명에 크기 조건을 걸거나 업로드 세션에 만료를 두는 방향이 어디까지 커버되는지는 아직 모른다.

## 정리할 때 — 경로만 보고 지우지 말 것

테스트로 남은 객체를 치우려다, 경로 패턴이 맞는 파일 5개가 사실 **한두 달 전 남의 데이터**인 걸 생성 시각 확인으로 알았다. 하마터면 날릴 뻔했다.

스토리지는 되돌리기가 없다. 지우기 전에 **"내가 만든 것인가"를 작업 시각 등 대조 가능한 축으로 확인**하는 절차를 넣어야 한다. 경로가 맞다는 건 근거가 못 된다.

> 상태: #wip — 실무에서 한 번 굴려보고 얻은 함정 모음. 다음 가지: [[CORS]] 별도 정리, 재개 가능 업로드(resumable), 업로드 세션·크기 조건 서명.

# 문서에 남길 것

설계 문서를 쓰다 보면 자꾸 길어진다. 함수 시그니처를 적고, 어느 파일에 뭘 추가할지 적고, 배경을 길게 서술한다. 그런데 **그렇게 쓴 부분이 제일 먼저 쓸모없어진다** — 구현이 끝나는 순간 코드가 정본이 되고, 문서의 그 부분은 코드와 어긋나기 시작한다.

핵심: **문서의 값어치는 분량이 아니라 코드를 읽어도 알 수 없는 것에서 나온다.**

## 남길 것 / 뺄 것

| 남길 것 | 왜 |
|---|---|
| **왜 이렇게 했는가** — 특히 검토했다 버린 대안 | 코드엔 채택된 선택지만 남는다. 버린 길은 코드에 흔적이 없다 |
| **지키지 않으면 깨지는 제약** | 나중에 "이거 왜 이렇게 불편하게 했지?"에 답한다 |
| **설계 원칙** — 어디까지가 이쪽 책임이고 무엇을 믿지 않는지 | 개별 함수를 다 읽어도 전체 그림은 안 보인다 |
| **아직 대응하지 않기로 한 리스크와 그 근거** | 나중에 증상이 나타났을 때 원인 추적 비용을 없앤다 |

| 뺄 것 | 왜 |
|---|---|
| 함수 시그니처·파일 목록 나열 | `grep` 이면 나온다. 그리고 금방 틀려진다 |
| 구현 절차 받아쓰기 | 코드가 정본이다 |
| 장황한 배경 서술 | 결론 먼저 한 줄, 근거는 필요한 만큼만 |

## 판별식

> **이 문장을 지웠을 때 다음 사람이 잘못된 결정을 내리는가.**

아니면 뺀다. 이 한 줄이 실제로 쓰이는 규칙이고 나머지는 그 부연이다.

실제로 겪은 예 — 어떤 값을 클라이언트에게서 **일부러 받지 않도록** 설계한 적이 있다. 문서에 "이 시그니처를 편의상 되돌리지 말 것"을 적어두지 않았다면, 다음 사람이 "인자로 받으면 편한데?"라고 생각했을 때 막을 근거가 없다. 이 문장은 판별식을 통과한다. 반면 같은 문서의 "`services/x.py` 에 함수 2개 추가" 같은 줄은 지워도 아무 결정이 바뀌지 않는다.

## 중복을 저장하지 않는다는 같은 원리

[[백링크]]에서 정리한 구조와 결이 같다. 거기서 링크는 **한쪽 파일에만 저장**되고 역방향은 저장하지 않는다 — 필요할 때 **계산**한다. 양쪽에 저장하면 둘이 어긋나는 순간 어느 쪽이 진실인지 알 수 없어진다.

문서도 마찬가지다. **코드에서 파생 가능한 것(시그니처·구조·절차)은 파생시키고**, 코드로부터 절대 계산되지 않는 것(왜·버린 대안·믿지 않기로 한 것)만 문서에 저장한다. 둘 다 적으면 문서와 코드가 갈라지고, 갈라진 문서는 읽는 사람을 잘못된 방향으로 이끈다 — 아예 없는 것보다 나쁘다.

## 실행 시점의 함정

구현이 끝난 뒤 문서를 다이어트하는 건 **별개 작업으로 분리**해야 한다. 기능 변경 커밋에 대규모 문서 삭제가 딸려 들어가면 리뷰어가 둘 다 제대로 못 본다. 지침을 세우는 것과 기존 문서를 정리하는 것은 다른 일이다.

> 상태: #wip — 다음 가지: 문서를 언제 버릴 것인가(계획서의 수명), 코드 주석과 문서의 역할 분담.

# 백링크

**"나를 링크한 노트들" 목록.** A 노트에 `[[B]]`라고 쓰면, B를 열었을 때 백링크 패널에 A가 뜬다.

여기서 계속 헷갈렸던 게 있다 — 그럼 B 파일에는 뭐가 저장되는가? **아무것도 저장되지 않는다.**

## 저장된 게 아니라 계산된 것

마크다운 파일을 열어 보면 링크는 **한쪽에만** 있다. `[[B]]`라는 텍스트는 A 파일 안에만 존재하고, B 파일은 A의 존재를 모른다. 파일 수준에서 링크는 철저히 **단방향**이다.

그럼 백링크는 어디서 오나. Obsidian이 볼트 전체를 스캔해 "누가 누구를 가리키나"를 인덱스로 들고 있다가, 그걸 **뒤집어서** 보여준다. 저장된 데이터가 아니라 **조회할 때 계산되는 뷰**다.

이 차이가 실제로 만드는 결과:

- A를 지우면 **B를 건드리지 않았는데도** B의 백링크 목록에서 A가 사라진다.
- 노트 이름을 바꾸면 Obsidian이 그 인덱스를 보고 가리키던 쪽들을 찾아 고쳐준다. 반대 방향은 고칠 게 없다 — 애초에 없으니까.

웹이랑 똑같은 구조다. 내가 남의 블로그를 링크해도 그 블로그 파일은 안 바뀐다. "이 페이지를 누가 링크했나"는 검색엔진이 따로 역인덱스를 만들어야 알 수 있다. 볼트는 규모가 작아서 그 역인덱스를 실시간으로 유지할 뿐이다.

**같은 발상이 DB에도 있다** — 원본 테이블은 그대로 두고 조회 방향을 뒤집으려고 따로 유지하는 자료구조가 인덱스다([[SQL 튜닝 MOC]]). 트레이드오프까지 닮았다. 조회는 공짜처럼 빨라지지만, 원본이 바뀔 때마다 인덱스를 갱신해야 한다. Obsidian도 볼트가 커질수록 이 스캔 비용을 낸다.

## 그래서 링크 방향은 "오염" 문제다

여기가 실제로 써먹은 지점.

링크를 쓰면 **쓴 쪽 파일만 더러워진다.** 받는 쪽은 파일이 그대로인데 조회는 된다. 그러니 링크 방향을 고를 때 진짜로 고르는 건 이거다 — **어느 노트의 본문을 더럽힐 것인가.**

이 볼트에서 일일 노트를 홈([[개발 세컨드 브레인]])에 붙일지 고민한 게 결국 이 문제였다. 개념 노트에서 "이거 언제 봤더라"를 알고 싶다고 개념 → 일일로 링크를 걸면, 개념 노트 본문이 `2026-07-29에 봄, 2026-08-03에 또 봄...` 하는 시간 로그로 뒤덮인다. 반대로 **일일 → 개념 한 방향만 걸면** 개념 노트는 깨끗한 채로 백링크에서 시간 정보가 다 나온다.

일반화하면 — **증식하는 쪽에서 안정된 쪽으로 건다.** 매일 늘어나는 일지가 화살표를 쏘고, 오래 남을 개념은 가만히 받기만 한다.

## 연결된 언급 / 연결되지 않은 언급

백링크 패널은 둘로 나뉜다.

- **연결된 언급** — 진짜 `[[링크]]`가 걸린 것.
- **연결되지 않은 언급** — 노트 제목과 같은 **문자열**이 본문에 있는데 링크는 안 걸린 것. 단순 텍스트 검색이라 `Authentication`이 `AuthenticationManager` 안에 들어 있는 식의 오탐이 섞인다.

후자는 "여기 링크 걸 걸 그랬나?" 하고 되묻는 용도다. 볼트 Lint 의 *상호참조 누락* 점검이 정확히 이걸 손으로 하는 것.

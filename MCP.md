# MCP

**LLM이 외부 도구·서비스를 부르라고 만든 표준 프로토콜**(Model Context Protocol). 처음 부딪히는 벽은 REST API와 비교하는 순간 온다 — REST는 "API 서버엔 엔드포인트가 있지"가 뼈대인데, MCP 서버엔 그 엔드포인트가 안 보인다. 이 차이를 이해하는 게 곧 MCP를 이해하는 것이다. 한 문장으로: **REST는 "주소(URL)"로 의도를 표현하고, MCP는 "메시지 내용(데이터)"으로 의도를 표현한다.**

## REST와 뭐가 다른가 — 문패 vs 민원 창구

**REST API = 문패가 붙은 건물.** `GET /users/42`, `POST /orders`처럼 방마다 문패(엔드포인트)가 있고, 원하는 방을 직접 찾아가 두드린다. 주소(경로) 자체가 "무슨 일로 왔는지"다.

**MCP 서버 = 민원 창구 하나.** 문패가 방마다 있는 게 아니라, 접수 창구가 딱 몇 개 정해져 있다.

- "여기서 **뭘 처리할 수 있어요?**" → `tools/list`
- "그중 **이거 처리해줘**" → `tools/call`

어떤 툴을 부를지는 **문패가 아니라 창구에 내미는 서류에 적어서** 낸다. `get_weather를 서울로`는 URL이 아니라 종이(JSON) 속 내용이다. 그래서 REST식으로 "툴 개수만큼 엔드포인트가 있겠지"라는 그림이 안 맞는 것.

## 실제로 오가는 메시지 — JSON-RPC 2.0

이 "서류 양식"이 표준화된 게 **JSON-RPC 2.0**이다. 날씨 툴 호출은 실제로 이런 메시지 하나로 간다.

```json
{
  "jsonrpc": "2.0",
  "id": 7,
  "method": "tools/call",         // 창구 이름 — 항상 고정
  "params": {
    "name": "get_weather",        // 진짜 부를 툴 = 여기 '데이터'로
    "arguments": { "city": "서울" }
  }
}
```

`method`는 늘 `tools/call`로 **고정**, 부르고 싶은 툴 이름은 `params` 안에 **값으로** 들어간다. REST였다면 `POST /get_weather`처럼 URL에 박혔을 게 봉투 속 내용물이 됐다. 이게 "주소가 아니라 데이터로 의도를 표현한다"의 실체.

그리고 이걸 **사람이 코드로 짜는 게 아니다.** REST는 개발자가 직접 `GET /users/42`를 호출하지만, MCP는 *LLM이* "이 툴 쓸래"라고 정하면 *클라이언트(예: Claude Code)가 자동으로* JSON-RPC로 번역해 보낸다. 그래서 MCP는 "사람이 부르는 API"가 아니라 **"LLM이 부르라고 만든 API"**다.

전체 흐름을 상태로 펼치면:

```
1. 연결       클라이언트 ↔ MCP서버  "initialize" (인사·기능 협상)
2. 목록 조회   클라이언트 → tools/list → 서버가 "쓸 수 있는 툴 목록 + 각 입력 스키마" 반환
3. LLM 주입   클라이언트가 그 목록을 LLM한테 "너 이런 도구 쓸 수 있어"라고 알려줌
4. LLM 결정   LLM이 "get_weather(서울) 쓸래" 판단   ← 여기가 LLM 몫
5. 번역·호출   클라이언트가 tools/call JSON으로 바꿔 서버에 전송
6. 실행·응답   서버 실행 → 결과 반환 → 클라이언트가 LLM에 다시 먹임
```

## 두 개의 통로 — stdio 파이프와 외부 조회는 별개

여기서 제일 헷갈렸던 지점. "MCP 서버가 로컬 프로세스라면(stdio) 외부 서비스 조회를 할 수 있나?" → **당연히 된다. 오히려 그게 대부분이다.** 통로 두 개를 분리해서 봐야 안 헷갈린다.

```
[내 컴퓨터]                                       [바깥 세상]
클라이언트  ──① stdio 파이프──  MCP서버 프로세스  ──② HTTP──▶  외부 API(Jira, 날씨…)
                (로컬, 주소 없음)   (평범한 프로그램)
```

- **① 클라이언트 ↔ MCP서버** — 이 통로만 파이프고 로컬이다. "네트워크 없음"은 오직 여기 얘기.
- **② MCP서버 ↔ 외부 세상** — 그냥 보통의 HTTP/DB 연결. stdio와 **아무 상관 없다.**

비유하면 창구 직원 — 나와는 좁은 유리창(①)으로만 얘기하지만, 유리창 뒤 직원은 전화·인터넷으로 중앙 전산망(②)을 조회한다. **유리창이 좁고 로컬이라고 직원이 바깥과 단절된 게 아니다.** 그래서 MCP 설정에 API 키·토큰을 환경변수로 넣어두고, 툴이 호출되면 서버가 그걸로 외부에 나가 조회한다. 유용한 MCP 서버는 대부분 "외부 서비스를 LLM이 쓰게 감싼 껍데기"인 이유가 이것.

## stdio vs HTTP — 서버가 로컬이냐 원격이냐

전송(transport) 방식은 두 갈래지만, 차이의 뿌리는 **"서버가 내 컴퓨터 안에 있고 내가 직접 켜나(stdio), 딴 데 떠 있고 접속만 하나(HTTP)"** 하나다. 나머지는 다 여기서 파생된다.

| | **stdio** | **Streamable HTTP** (원격) |
|---|---|---|
| 서버 위치 | 로컬 | 어디든 (원격 가능) |
| 누가 띄우나 | 클라이언트가 자식 프로세스로 실행 | 이미 떠 있음, 접속만 |
| 통신 | stdin/stdout 파이프 | HTTP POST (+필요시 SSE 스트리밍) |
| 주소 | 없음 (실행할 *명령어*로 지정) | URL 있음 |
| 수명 | 클라이언트가 켜고 끔 (나와 생사 같이) | 서버 독립 생존 |
| 인증 | 대개 불필요 | 필요 (OAuth 등) |
| 공유 | 1:1, 내 전용 | N:1, 여러 클라이언트 |

**stdio = 내가 부르는 개인 비서.** 세션 시작하면 클라이언트가 `npx some-mcp-server` 같은 명령을 실제로 실행해 프로세스를 낳고, 세션 끝나면 죽인다. 옆자리(로컬)라 주소도 인증도 필요 없다.

**HTTP = 로비 안내데스크.** 내가 오든 말든 늘 열려 있고, URL로 원격 접속하며, 아무나 물어볼 수 있으니 신분 확인(인증)이 필수다. 그래서 원격 MCP는 OAuth를 요구한다 — 로컬(stdio)이면 이미 내 사람이라 인증 절차가 없고, 원격이면 남의 서버라 필요해지는 것.

> 실전 감별법: **OAuth 하라고 뜨면 원격 HTTP, 조용히 그냥 되면 대개 로컬 stdio.**

## 각주 — SSE와 HTTP 전송의 두 세대

원격 방식엔 세대가 있는데, 여기서 **SSE(Server-Sent Events)** 를 오해하기 쉽다. SSE는 서버→클라 **단방향 푸시** 기술(라디오 방송)일 뿐, 그 자체가 MCP 전송 방식이 아니다. MCP가 이걸 *어떻게 쓰느냐*가 갈린다.

- **HTTP+SSE (옛)** — SSE를 상시 열어둬 결과를 받고(downlink) + 별도 POST로 요청을 보내는(uplink) **2채널** 구조. 서버가 클라마다 장기 연결을 물고 있어야 해서 **stateful** — 로드밸런서·서버리스에 부적합(sticky 필요).
- **Streamable HTTP (현 표준)** — POST 엔드포인트 하나로 합치고, 대부분은 그냥 JSON 응답 한 방으로 끝낸다. 스트리밍이 필요할 때만 그 응답을 SSE로 켠다. 평소엔 평범한 요청-응답이라 **stateless** — 아무 서버 인스턴스나 처리 가능.

즉 SSE가 사라진 게 아니라 "상시 별도 채널"에서 "필요할 때만 켜는 옵션"으로 강등된 것. 실무 정리: **로컬=stdio, 원격=Streamable HTTP** (HTTP+SSE는 deprecated).

> 상태: #wip — REST 대비 / 두 통로 / 전송 방식까지. 다음 가지: `resources`·`prompts`(툴 말고 다른 노출 방식), 인증/권한 상세, [[REST API]]와의 본격 대조.

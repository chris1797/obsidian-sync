# uv

Python **버전**과 **가상환경·패키지**를 한 도구로 관리하는 매니저. 이 머신은 pyenv·asdf·mise·conda 없이 uv 하나로 통일했고, 시스템 `/usr/bin/python3`(3.9.6)는 건드리지 않는다. 도구를 늘린 게 아니라 **경로를 하나로 줄인 것**이 요점 — 파이썬이 어디서 왔는지 헷갈릴 여지를 없앤다.

## 활성화하지 않는다

가장 크게 바뀐 습관. 예전엔 `source .venv/bin/activate` 로 **현재 셸의 기본 파이썬을 바꿔놓고**, 뒤따르는 명령들이 그걸 물게 했다. uv 는 명령마다 대상을 적는다.

```sh
uv pip install --python "$TARGET/.venv/bin/python" -r requirements.txt
```

차이는 타이핑 길이가 아니라 **암묵적 전역 상태에 기대느냐**다. `activate` 는 "지금 어느 환경이 켜져 있나"를 셸 안에 숨겨두고, 스크립트는 그게 켜져 있다고 가정한다. 가정이 틀리는 순간 패키지가 시스템 파이썬에 깔린다. 그리고 틀렸다는 걸 알려주는 신호가 없다 — 명령은 조용히 성공한다.

[[kubeconfig 와 컨텍스트]]에서 `kubectl --context` 를 스크립트의 정석으로 꼽은 것과 **정확히 같은 구조**다. 거기서도 현재 컨텍스트를 바꾸지 않고 이번 명령만 다른 클러스터로 보냈다 — 실행 시점의 기본값을 가정하지 않으려고. `activate` vs `--python` 도 같은 선택이고, 착각의 대가도 같다(엉뚱한 대상에 쓰기).

## venv 만드는 방식이 다르다

```sh
uv venv --python 3.12 .venv
```

`python -m venv` 는 새 환경 **안에 pip 을 심어주고**(ensurepip), 그 pip 이 대체로 낡아서 `pip install --upgrade pip` 를 한 번 더 돌리는 게 관례였다. uv 는 설치를 자기가 하므로 환경 안에 pip 이 **없어도 된다.** 부트스트랩 단계가 통째로 사라지는 이유가 이것 — 빨라진 게 아니라 할 일이 없어진 것.

## `python3` 가 3.9.6 으로 뜨는 건 정상

uv 가 PATH 에 노출하는 건 버전 붙은 이름(`python3.12`)뿐이라, 맨 `python3` 는 여전히 시스템 것이다. 여기서 "설치가 안 됐나" 하고 헤매기 쉬운데, **시스템 파이썬을 건드리지 않는 게 설계 의도**다. 프로젝트 작업은 항상 `.venv/bin/python` 을 본다.

같은 맥락에서 `.venv/bin/python` 이 `No such file or directory` 로 죽는 일이 생긴다. venv 는 베이스 인터프리터를 **심볼릭 링크로 가리킬 뿐**이라([[심볼릭 링크로 배포한 설정]]), 그 베이스를 지우면(brew python 삭제, uv 버전 정리) 링크가 허공을 가리킨다. venv 자체는 멀쩡해 보이는데 파이썬만 없는 상태 — 재생성 말고는 답이 없다.

## [[GIL]] 과 이어지는 곳

[[GIL]]에 남겨둔 의문 — Python 3.13 free-threaded 빌드가 레퍼런스 카운팅 문제를 어떻게 푸는가 — 을 실제로 만져보려면 그 **특수 빌드**를 받아야 한다. 일반 배포판이 아니라 GIL 을 뺀 별도 컴파일이라, 예전 같으면 소스 빌드나 별도 설치 경로를 뒤졌을 일이다.

버전 매니저가 하나로 통일돼 있으면 이게 한 줄이다.

```sh
uv python install 3.13t     # t = +freethreaded
```

런타임 실험의 **진입 비용을 낮추는 것**이 버전 매니저의 숨은 값어치다. "궁금한데 환경 만들기 귀찮아서 안 해봄" 이 사라진다.

> 상태: #wip — `uv.lock`·`pyproject.toml` 기반 프로젝트 모드(`uv sync`, `uv run`)는 아직 안 썼다. `uv pip` 은 requirements.txt 호환 레이어일 뿐이라 그쪽이 본체에 가깝다.

# Pod

[[Kubernetes]] 배포의 **최소 단위**. 여기서 첫 번째로 걸리는 게 이거다 — 우리는 "컨테이너를 배포한다"고 생각하는데, k8s는 컨테이너를 직접 다루지 않는다. 항상 **Pod라는 껍데기로 한 번 감싸서** 다룬다. `docker run`으로 컨테이너 하나 띄우던 감각에서 보면 왜 굳이 한 겹을 더 씌우나 싶다. 그 이유를 이해하는 게 곧 Pod를 이해하는 것이다.

## 왜 컨테이너가 아니라 Pod인가

핵심은 **"같이 살고 같이 죽어야 하는" 컨테이너 묶음**이라는 발상이다. 컨테이너 하나가 배포 단위라면, 서로 뗄 수 없이 붙어 있어야 하는 두 프로세스를 어떻게 묶을까? 예를 들어 앱 컨테이너와, 그 앱의 로그를 실시간으로 수집해 전송하는 보조 컨테이너. 이 둘은

- 같은 노드에 떠야 하고 (다른 노드에 떨어지면 로컬 로그를 못 읽는다)
- 같은 생명주기를 가져야 하고 (앱이 죽으면 로그 수집도 의미 없다)
- 파일·네트워크를 공유해야 한다

이걸 "컨테이너 = 배포 단위"로는 표현할 방법이 없다. 그래서 k8s는 **한 단계 위의 묶음**을 만들었다. Pod 안의 컨테이너들은 위 세 조건을 자동으로 만족한다. 즉 Pod는 *"하나의 논리적 호스트"* — 예전 같으면 한 대의 물리 서버에 나란히 띄웠을 프로세스들을, 하나의 단위로 묶은 것이다.

## Pod 안의 컨테이너가 공유하는 것

같은 Pod에 든 컨테이너들은 두 가지를 공유한다. 이게 Pod의 실체다.

- **네트워크** — Pod 하나당 IP 하나. 안의 컨테이너들은 그 IP를 나눠 쓰고, 서로를 `localhost`로 부른다. 포트만 다르면 된다. (내부적으로는 `pause` 컨테이너라는 눈에 안 보이는 컨테이너가 네트워크 네임스페이스를 잡아두고, 나머지가 거기 올라타는 구조다.)
- **스토리지 볼륨** — Pod에 붙인 볼륨을 여러 컨테이너가 같이 마운트한다. 위 로그 예시에서 앱이 파일에 로그를 쓰고, 수집기가 같은 파일을 읽는 게 이걸로 된다.

CPU·메모리·프로세스 공간은 컨테이너별로 여전히 격리된다. **네트워크와 스토리지만 공유**하는 것 — "한 호스트에 나란히 있는 것처럼"의 딱 그만큼이다.

## 보통은 컨테이너 1개 — 사이드카가 예외

오해하기 쉬운데, "Pod = 컨테이너 여러 개"가 기본이 아니다. **대부분의 Pod는 컨테이너 1개**다. 여러 개를 넣는 건 위처럼 *정말 뗄 수 없을 때만* 쓰는 패턴이고, 이걸 **사이드카(sidecar)** 라 부른다. 로그 수집, 프록시, 설정 동기화 같은 보조 역할을 본체 옆에 붙이는 것.

판단 기준은 하나다 — **따로 scale할 수 있으면 다른 Pod로 쪼개라.** 앱은 3개, 로그 수집기는 1개면 되는 관계라면 사이드카가 아니라 별도 배포다. 같이 늘고 같이 줄어야 하는 것만 한 Pod에 넣는다.

## Pod는 소모품이다 — 되살아나지 않는다

여기가 [[컨트롤 루프]]와 이어지는 지점이자, Pod의 성격을 가장 잘 드러내는 부분이다. **Pod는 고쳐 쓰는 게 아니라 버리고 새로 만드는 것**이다. 애완동물(pet)이 아니라 가축(cattle) — 한 마리가 아프면 치료하는 게 아니라 교체한다.

- Pod가 죽으면 그 Pod가 *되살아나지* 않는다. 대신 [[컨트롤 루프]]가 "3개여야 하는데 2개네" 하고 **완전히 새 Pod**를 띄운다.
- 새 Pod는 **새 IP**를 받는다. 이름도 대개 랜덤 접미사가 붙어 바뀐다.
- 그래서 Pod를 직접 만들어 쓰는 일은 거의 없다. [[Deployment]] 같은 상위 오브젝트에게 "이 Pod를 N개 유지해"라고 맡기고, 죽고 살리는 건 컨트롤러가 한다.

이 "Pod IP가 계속 바뀐다"는 성질이 바로 [[Service]]가 존재하는 이유다. 흔들리는 Pod들 앞에 고정된 진입점을 세워야 다른 데서 안정적으로 찾을 수 있으니까. Pod의 덧없음(mortality)이 Service의 필요를 낳는 셈이다.

## 계층 속 Pod의 자리

[[Kubernetes]] 지도에서 봤던 오브젝트 계층을 Pod 기준으로 다시 보면 흐름이 선명하다.

```
Deployment  →  ReplicaSet  →  Pod  →  컨테이너(+공유 네트워크/볼륨)
 (버전·롤백)    (N개 유지)     (배포 최소 단위)
```

위로 갈수록 "얼마나 오래 살아야 하나"의 책임을 지고, 아래로 갈수록 "실제로 뭘 실행하나"에 가깝다. Pod는 그 경계에 있다 — **선언의 세계(YAML로 원하는 상태)와 실행의 세계(노드 위 실제 컨테이너)가 만나는 최소 단위.** [[컨트롤 루프|Scheduler]]가 "이 Pod를 어느 노드에" 배정하고, 그 노드의 kubelet이 Pod 안 컨테이너를 실제로 띄운다.

> 상태: #wip — Pod의 정체성(공유·소모품성)까지. 다음 가지: Pod의 lifecycle phase(Pending/Running/Succeeded/Failed), readiness/liveness probe(컨트롤 루프가 "살아있음"을 어떻게 판정하는가), init container.

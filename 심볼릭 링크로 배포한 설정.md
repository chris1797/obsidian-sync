# 심볼릭 링크로 배포한 설정

AI 코딩 에이전트의 규칙·프롬프트를 여러 프로젝트에서 같이 쓰려고 한 레포(하네스)에 모아두고, 홈 디렉터리의 설정 경로에서 그 레포를 **심볼릭 링크로 물려**뒀다. 그러다 규칙을 한 줄 고치고 "커밋해야 적용되나?"를 잠깐 헷갈렸다. 답은 **아니다 — 저장하는 순간 이미 적용돼 있다.**

## 이름 둘, 실체 하나

심볼릭 링크는 복사가 아니다. `~/.claude/shared` → `<레포>/shared` 로 걸어두면, 앞 경로로 열든 뒤 경로로 열든 **디스크의 같은 파일 한 덩어리**를 연다. 파일에 붙은 이름표가 두 개일 뿐 실체는 하나다.

그래서 편집기가 저장을 끝낸 시점에 반영이 끝난다. 커밋·푸시는 **적용 조건이 아니라 영속화와 이력**이다 — 다른 기기로 옮기고, 되돌릴 수 있게 만드는 일. 이 둘을 섞으면 "아직 커밋 안 했으니 안 먹었겠지" 같은 잘못된 진단을 하게 된다. 반대로 "커밋했으니 됐다"도 틀릴 수 있다. 적용을 결정하는 건 **경로가 실체에 닿아 있느냐** 하나뿐이다.

## 판정법 — inode 비교

```sh
stat -f '%i %N' ~/.claude/shared/guides/coding.md <레포>/shared/guides/coding.md   # macOS
stat -c '%i %n' ...                                                                  # GNU
```

앞에 찍히는 숫자가 **inode** — 파일시스템이 실체에 붙인 일련번호다. 두 경로의 inode 가 같으면 같은 파일이고, 다르면 사본이라 한쪽을 고쳐도 다른 쪽은 그대로다.

`ls -l` 로 화살표(`->`)를 보는 것보다 이쪽이 확실하다. **링크가 파일이 아니라 중간 디렉터리에 걸려 있으면**, 그 안의 파일에 `ls -l` 을 해봐야 화살표 없는 평범한 파일로 보이기 때문이다. 실제로 이걸로 한 번 속았다 — 파일만 보고 "사본이구나" 했는데, 링크는 상위 디렉터리에 걸려 있었다.

## 왜 복사가 아니라 링크인가

[[문서에 남길 것]]에서 정리한 **"중복을 저장하지 않는다"** 의 파일시스템 판이다. 같은 내용을 두 곳에 저장하면 갈라지는 순간 어느 쪽이 정본인지 알 수 없다. [[백링크]]가 역방향을 저장하지 않고 계산하듯, 심볼릭 링크는 실체를 한 곳에만 두고 나머지 이름은 가리키기만 한다. 정본이 하나면 "동기화" 라는 작업 자체가 사라진다.

에이전트 하네스에서 이 성질이 특히 값싸게 먹힌다. 규칙 파일 하나를 고치면 그 링크를 물고 있는 모든 프로젝트 세션에 즉시 반영된다 — [[MCP]] 서버 설정처럼 프로젝트마다 복사해 두었다면 하나 고칠 때마다 전부 따라 고쳐야 했을 것이다.

## 반대편 함정과 짝

[[kubeconfig 와 컨텍스트]]에서 겪은 사고는 설정이 **읽히지 않는 파일**에 적혀 있던 경우였다. 이번 건 그 반대편이다 — 경로는 제대로 읽히는데 **실체가 사본**이라 고쳐도 안 먹는다.

그래서 설정이 안 먹을 때 물어볼 게 두 개로 정리된다.

1. 이 파일이 **실제로 읽히는 파일**인가 (kubeconfig 쪽 함정)
2. 내가 고친 파일이 **그 파일과 같은 실체**인가 (이쪽 함정)

값이 맞는지부터 들여다보는 건 대개 셋째 순서다.

> 상태: #wip — 하드 링크와의 차이(inode 를 공유하되 이름 삭제 시 동작이 다름), 링크가 깨졌을 때(dangling) 도구들이 어떻게 반응하는지는 아직 안 팠다.

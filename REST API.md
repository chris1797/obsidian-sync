# REST API

자원(resource)을 **URL 주소로 지정하고, HTTP 동사로 조작하는** API 설계 스타일. 이 노트는 [[MCP]]를 이해하려다 거꾸로 또렷해진 개념이다 — MCP가 "메시지 내용으로 의도를 표현"한다면, REST는 **"주소로 의도를 표현"**한다. 둘을 나란히 놓으면 서로가 선명해진다.

## 뼈대 — 자원은 명사(URL), 행위는 동사(HTTP method)

- **URL = 무엇을** (자원): `/users/42`, `/orders`
- **HTTP 동사 = 어떻게** (행위): `GET` 조회 · `POST` 생성 · `PUT`/`PATCH` 수정 · `DELETE` 삭제

`GET /users/42` = "42번 유저를 조회". 주소가 대상을, 동사가 행위를 짊어진다. 그래서 **엔드포인트가 자원 × 행위 조합만큼 늘어난다** — 문패가 방마다 붙은 건물. [[MCP]]의 "창구 하나(`tools/call`)에 서류로 뭘 할지 적어 낸다"와 정반대 방향이다.

## 무상태(stateless)

각 요청은 자기 완결적이다 — 서버가 이전 요청을 기억하지 않는다. 필요한 인증·컨텍스트를 **매 요청에 담아** 보낸다. 그 대가로 얻는 게 큰데, 서버가 상태를 안 들고 있으니 **아무 인스턴스나 요청을 받아 처리**할 수 있다(수평 확장). 이 동기는 [[MCP]] 각주의 Streamable HTTP가 stateless를 지향한 이유와 정확히 같다.

## 같은 HTTP인데 뭐가 다른가 — 부르는 주체

REST와 [[MCP]] 원격 전송은 둘 다 HTTP 위에 있다. 결정적 차이는 **누가 부르느냐**다.

- **REST** — *개발자가* 코드로 직접 `GET /users/42`를 짠다.
- **MCP** — *LLM이* "이 툴 쓸래"라고 정하면, 클라이언트가 대신 JSON-RPC로 번역해 호출한다.

그래서 REST는 "사람이 부르는 API", MCP는 "LLM이 부르라고 만든 API". 인터페이스 설계의 청중이 사람이냐 모델이냐가 갈린다.

> 상태: #wip — REST의 기본 뼈대와 MCP 대조. 다음 가지: 멱등성(idempotency, 같은 요청 여러 번 = 같은 결과), 상태 코드 규약, 버저닝, HATEOAS.

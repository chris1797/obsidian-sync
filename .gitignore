.DS_Store

# Obsidian: per-device UI state (auto-updated on every focus/close)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# obsidian-git plugin: transient conflict marker file
conflict-files-obsidian-git.md

# Claude Code: per-device permission state (auto-updated as permissions accumulate)
.claude/settings.local.json

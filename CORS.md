# CORS

**브라우저가 강제하는 정책**이다. 서버 권한과 아무 상관이 없다. 이 한 줄을 몰라서 [[GCS 직접 업로드]]를 붙이다 한참 헤맸다.

증상은 늘 똑같다 — `curl`로는 200이 나오는데, 같은 요청을 브라우저에서 보내면 콘솔에 `blocked by CORS policy`가 뜬다. 자격증명도 서명도 멀쩡한데. **막는 주체가 서버가 아니라 브라우저**라서 그렇다.

## 뒤집어 이해하기 — CORS는 푸는 장치다

이름 때문에 "차단 정책"으로 읽히는데 반대다. 기본값이 이미 차단이고(**동일 출처 정책**, Same-Origin Policy), CORS는 그 **빗장을 조건부로 푸는** 메커니즘이다. `Cross-Origin Resource **Sharing**` — 이름에 공유가 들어 있는 이유다.

왜 기본이 차단인가. 브라우저가 **쿠키를 자동으로 실어 보내기** 때문이다. 내가 은행에 로그인한 상태에서 악성 사이트를 열었을 때, 그 사이트의 스크립트가 은행 API를 부르면 브라우저는 친절하게 내 세션 쿠키를 붙여 보낸다. 응답을 스크립트가 읽을 수 있으면 잔액이 유출된다. 그래서 브라우저는 **다른 출처의 응답을 스크립트에 넘겨주지 않는 것**을 기본으로 삼는다.

즉 CORS가 보호하는 대상은 서버가 아니라 **사용자**다. 서버를 지키는 건 여전히 인증·인가의 몫이다([[SpringSecurity]]). 이 둘을 섞으면 "CORS를 열면 위험한 것 아닌가?"라는 잘못된 걱정을 하게 된다 — 여는 건 "이 출처의 스크립트가 응답을 읽어도 된다"는 선언일 뿐이다.

## 출처(origin)는 문자열이다

`scheme + host + port` 세 개가 모두 같아야 같은 출처다. **하나라도 다르면 남**이다.

```
http://localhost:3000   ≠   http://127.0.0.1:3000    ← host 문자열이 다름
http://example.com      ≠   https://example.com      ← scheme 이 다름
https://example.com     ≠   https://api.example.com  ← 서브도메인도 남
```

여기서 나온 실용적 함정: 서버가 허용 목록과 **문자열을 대조할 뿐, 상대 호스트로 접속하지 않는다.** 그래서 `localhost`를 목록에 넣으면 **로컬 개발 환경도 그대로 통과한다** — 외부 서비스가 내 노트북에 들어올 필요가 없으니까. 반대로 주소창에 `127.0.0.1`을 치면 등록해둔 `localhost`와 안 맞아 막힌다. 같은 서버인데도.

## preflight — 여기에 인증이 안 실린다

모든 요청이 미리 물어보는 건 아니다. 단순한 요청(`GET`, 그리고 폼 전송 수준의 `POST`)은 **일단 보내고 응답 헤더로 판정**한다. 그런데 다음 중 하나라도 해당하면 브라우저가 **먼저 `OPTIONS`로 물어본다**:

- `PUT`·`DELETE`·`PATCH` 같은 메서드
- `Content-Type`을 `application/json`으로 지정하는 등 커스텀 헤더를 붙일 때

이게 preflight다. 그리고 결정적으로 — **preflight에는 인증 정보가 실리지 않는다.** 브라우저가 자동 생성하는 익명 요청이고, 서버도 권한을 확인하지 않고 CORS 설정만 보고 답한다.

```
브라우저 ──▶ OPTIONS (Origin, Access-Control-Request-Method)   ← 인증 없음
        ◀── 200 인데 Access-Control-Allow-Origin 헤더가 없음
브라우저     실제 요청을 발사조차 하지 않음
```

**허용 헤더가 없으면 진짜 요청은 나가보지도 못한다.** 그래서 권한이 아무리 좋아도 소용이 없고, 디버깅할 때 서버 접근 로그를 봐도 요청 자체가 안 찍힌다. 이 지점이 제일 헷갈린다.

또 하나 — preflight 결과는 캐시된다(`Access-Control-Max-Age`). CORS를 방금 열었는데 안 되는 것 같으면, 직전 실패가 캐시된 것일 수 있다. 시크릿 창으로 확인하는 게 빠르다.

## 서버가 하는 일은 응답 헤더 붙이기뿐

서버 쪽은 코드가 아니라 **설정**인 경우가 많다. [[REST API]]를 짤 때 별도 로직으로 CORS를 구현한다기보다, 프레임워크나 인프라(오브젝트 스토리지·게이트웨이 등)에서 허용 목록을 선언한다.

주의할 조합: 쿠키를 함께 보내는 요청(`credentials: 'include'`)이면 서버가 `Access-Control-Allow-Origin: *`으로 답할 수 없다. **와일드카드와 자격증명은 같이 못 쓴다** — 정확한 출처를 명시해야 한다. 기본이 차단인 이유(쿠키 자동 첨부)를 생각하면 당연한 제약이다.

## 자주 하는 오해 정리

- ~~CORS 에러 = 권한 부족~~ → **브라우저 판정**. 권한과 무관하다.
- ~~서버에서 CORS를 풀면 아무나 접근 가능해진다~~ → 접근 통제는 여전히 인증·인가 몫. CORS는 "브라우저가 응답을 스크립트에 넘겨도 되는가"만 정한다.
- ~~프론트 코드로 우회할 수 있다~~ → 브라우저가 강제하므로 클라이언트 코드로는 못 푼다. 서버가 열거나, 서버 간 호출(프록시)로 브라우저를 빼야 한다.
- ~~CSP를 열었으니 됐다~~ → 다른 층이다. **CSP는 "우리 페이지가 어디로 보낼 수 있나"(우리가 정함), CORS는 "상대가 우리를 받아주나"(상대가 정함).** 둘 다 뚫려야 요청이 성립한다.

> 상태: #wip — [[GCS 직접 업로드]]를 붙이며 정리. 다음 가지: CSP 별도 노트, simple request 판정 조건의 정확한 목록, 프록시로 CORS를 피하는 패턴의 트레이드오프.
